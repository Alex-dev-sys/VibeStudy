# 🚀 Запуск проекта для разработки

## Быстрый старт

### Одна команда для всего:

```bash
npm run dev
```

Эта команда автоматически запустит:
- ✅ Next.js dev сервер (http://localhost:3000)
- ✅ Telegram бот (polling режим)

Вы увидите:
```
🚀 Запуск VibeStudy с Telegram ботом...

   ▲ Next.js 14.2.8
   - Local:        http://localhost:3000

 ✓ Ready in 2.3s

🤖 Запуск Telegram бота...

✅ Бот подключен: @study_vibe_bot
⏳ Ожидание сообщений...
```

---

## 📋 Доступные команды

| Команда | Что делает |
|---------|------------|
| `npm run dev` | 🚀 Запуск сервера + бота (рекомендуется) |
| `npm run dev:next` | Только Next.js сервер (без бота) |
| `npm run bot` | Только Telegram бот |
| `npm run build` | Сборка для production |
| `npm run start` | Запуск production сборки |
| `npm run lint` | Проверка кода |

---

## 🎯 Что работает после запуска

### 1. Веб-сайт
- Откройте: http://localhost:3000
- Доступны все страницы: главная, обучение, профиль, playground

### 2. Telegram бот
- Откройте: https://t.me/study_vibe_bot
- Нажмите Start
- Отправьте команды: `/help`, `/stats`, `/advice`

### 3. ИИ функции
- Генерация заданий
- Проверка кода
- Подсказки
- ИИ-помощник в чате

---

## ⏹️ Остановка

Нажмите `Ctrl+C` в терминале — остановятся и сервер, и бот:

```
^C
👋 Остановка сервера и бота...
```

---

## 🔧 Настройки

### Переменные окружения (.env.local)

```env
# Hugging Face API (для ИИ функций)
HF_API_KEY=your_huggingface_key

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token
CRON_SECRET=your_secret_key
```

---

## 🐛 Troubleshooting

### Порт 3000 занят

```bash
# Найдите процесс
lsof -ti:3000

# Убейте процесс
kill -9 $(lsof -ti:3000)

# Или используйте другой порт
PORT=3001 npm run dev
```

### Бот не запускается

Запустите только бота для отладки:
```bash
npm run bot
```

Проверьте токен в `.env.local`

### Ошибки TypeScript

```bash
# Очистите кеш
rm -rf .next
npm run dev
```

---

## 📚 Дополнительная документация

- `README.md` — Общая информация о проекте
- `AI_FEATURES.md` — Документация по ИИ функциям
- `TELEGRAM_BOT_SETUP.md` — Полная настройка Telegram бота
- `TELEGRAM_LOCAL_RUN.md` — Локальный запуск бота

---

## ✨ Советы

1. **Держите терминал открытым** — вы будете видеть логи сервера и бота
2. **Проверяйте логи бота** — все сообщения отображаются в консоли
3. **Hot reload работает** — изменения в коде применяются автоматически
4. **Бот работает в фоне** — не нужно открывать второй терминал

---

**Готово! Просто запустите `npm run dev` и начните разработку! 🎉**

