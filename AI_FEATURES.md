# 🤖 ИИ-функции VibeStudy

## Обзор

В проект добавлена глубокая интеграция с искусственным интеллектом через Hugging Face API. Система включает автоматическую проверку кода, персональные подсказки, адаптивный учебный план и детальную аналитику.

---

## 🎯 Основные возможности

### 1. **ИИ-рефери для кода** (`/api/check-solution`)

Автоматическая проверка решений студентов с детальной обратной связью.

**Возможности:**
- ✅ Определение корректности решения
- 📊 Оценка от 0 до 100 баллов
- 💬 Детальная обратная связь по коду
- 💡 Конкретные рекомендации по улучшению
- 🎯 Учёт уровня сложности задачи

**Пример использования:**
```typescript
const response = await fetch('/api/check-solution', {
  method: 'POST',
  body: JSON.stringify({
    code: userCode,
    task: { title, description, difficulty },
    languageId: 'python'
  })
});

const result = await response.json();
// { success: true, message: "...", feedback: "...", suggestions: [...], score: 85 }
```

---

### 2. **Система подсказок** (`/api/get-hint`)

Умные подсказки, адаптирующиеся к количеству попыток и ошибкам студента.

**Возможности:**
- 💡 Подсказки разного уровня детализации
- 📝 Примеры кода (без полного решения)
- ✨ Пошаговые инструкции
- 🎯 Учёт предыдущих ошибок

**Логика подсказок:**
- **Попытка 1-2:** Общее направление
- **Попытка 3+:** Более детальная помощь с примерами
- **При ошибке:** Объяснение причины ошибки

---

### 3. **ИИ-помощник для теории** (`/api/explain-theory`)

Интерактивный чат-помощник для объяснения концепций.

**Возможности:**
- 📚 Объяснение теории простым языком
- 💻 Практические примеры на выбранном языке
- 🔗 Связанные темы для углубления
- 🎓 Адаптация под уровень студента (день курса)

**Интерфейс:**
- Плавающая кнопка 🤖 в правом нижнем углу
- Полноэкранный чат с историей сообщений
- Быстрые шаблоны вопросов

---

### 4. **Профиль знаний** (`knowledge-profile-store`)

Система отслеживания прогресса и слабых мест студента.

**Отслеживаемые метрики:**
- 📊 История всех попыток решения задач
- ⏱️ Время на каждую задачу
- 💡 Количество использованных подсказок
- ⭐ Баллы за каждое решение
- ❌ Типы ошибок

**Структура данных:**
```typescript
interface TaskAttempt {
  taskId: string;
  day: number;
  languageId: string;
  attempts: number;
  hintsUsed: number;
  timeSpent: number; // секунды
  completed: boolean;
  score: number; // 0-100
  errors: string[];
  timestamp: number;
}
```

---

### 5. **Адаптивные рекомендации** (`AdaptiveRecommendationsPanel`)

Панель с персональными рекомендациями на основе анализа прогресса.

**Функции:**
- 🎯 Рекомендуемая сложность заданий (easy/medium/hard)
- 🔄 Темы для повторения
- ⚠️ Слабые места с процентом ошибок
- 📋 Конкретные действия для улучшения

**Алгоритм рекомендаций:**
- Средний балл < 50 → Лёгкие задания
- Средний балл 50-80 → Средние задания
- Средний балл > 80 → Сложные задания

**Определение слабых мест:**
- Процент неудач > 40% → Требует внимания
- Процент неудач > 70% → Повторить теорию
- Много попыток + высокий процент неудач → Пропустить и вернуться позже

---

### 6. **Детальная аналитика** (`AnalyticsPanel`)

Визуализация статистики обучения с рекомендациями.

**Метрики:**
- ✅ Процент успешных решений
- ⏱️ Среднее время на задачу
- 🔄 Среднее количество попыток до успеха
- 💡 Среднее количество подсказок на задачу
- 📈 Тренд последних 10 задач
- 🔍 Топ-5 частых ошибок

**Персональные советы:**
- Процент успеха < 50% → "Не спешите! Используйте подсказки"
- Много подсказок → "Попробуйте решать самостоятельно"
- Быстрое решение → "Отличный темп! Вникайте в детали"

---

## 🛠️ Технические детали

### API-эндпоинты

Все эндпоинты используют Hugging Face Chat Completions API:

```typescript
const response = await fetch('https://router.huggingface.co/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${process.env.HF_API_KEY}`
  },
  body: JSON.stringify({
    model: 'openai/gpt-oss-20b:groq',
    messages: [...],
    temperature: 0.7-0.8,
    max_tokens: 1000-2000
  })
});
```

### Хранение данных

Используется Zustand с persist middleware:

```typescript
export const useKnowledgeProfileStore = create<KnowledgeProfileStore>()(
  persist(
    (set, get) => ({
      // state и actions
    }),
    {
      name: 'knowledge-profile-storage',
      version: 1
    }
  )
);
```

Данные сохраняются в `localStorage` браузера.

---

## 📊 Интеграция в UI

### TaskModal (Модальное окно задачи)

**Новые функции:**
- Кнопка "💡 Подсказка" → Запрос к `/api/get-hint`
- Кнопка "✓ Проверить" → Запрос к `/api/check-solution`
- Автоматическое сохранение попыток в профиль знаний
- Отображение рекомендаций по улучшению
- История использованных подсказок

### LearningDashboard (Главная страница обучения)

**Новые компоненты:**
- `AdaptiveRecommendationsPanel` — панель рекомендаций
- `AIAssistantChat` — плавающий чат-помощник (кнопка 🤖)

### ProfilePage (Страница профиля)

**Новые секции:**
- `AnalyticsPanel` — детальная аналитика прогресса

---

## 🎨 Дизайн-система

### Цветовая схема

```css
/* Успех */
.success { border-color: rgb(16 185 129 / 0.4); background: rgb(16 185 129 / 0.1); }

/* Ошибка */
.error { border-color: rgb(244 63 94 / 0.4); background: rgb(244 63 94 / 0.1); }

/* Подсказки */
.hint { border-color: rgb(168 85 247 / 0.4); background: rgb(168 85 247 / 0.1); }

/* Рекомендации */
.recommendation { border-color: rgb(59 130 246 / 0.4); background: rgb(59 130 246 / 0.1); }
```

### Анимации

Используется Framer Motion:

```typescript
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
  {/* content */}
</motion.div>
```

---

## 🚀 Использование

### Настройка

1. Убедитесь, что в `.env.local` установлен `HF_API_KEY`:
   ```
   HF_API_KEY=your_huggingface_api_key
   ```

2. Перезапустите dev-сервер:
   ```bash
   npm run dev
   ```

### Работа с системой

1. **Решение задач:**
   - Откройте задачу
   - Напишите код
   - Нажмите "✓ Проверить" для получения оценки
   - При необходимости используйте "💡 Подсказка"

2. **ИИ-помощник:**
   - Нажмите 🤖 в правом нижнем углу
   - Задайте вопрос по теории
   - Используйте быстрые шаблоны

3. **Адаптивные рекомендации:**
   - Автоматически появляются после первых попыток
   - Следуйте рекомендациям для оптимального прогресса

4. **Аналитика:**
   - Перейдите в Профиль
   - Изучите детальную статистику
   - Обратите внимание на частые ошибки

---

## 📈 Метрики и KPI

### Для студента

- **Процент успеха** — % правильно решённых задач
- **Средний балл** — качество решений (0-100)
- **Время на задачу** — эффективность решения
- **Использование подсказок** — самостоятельность

### Для системы

- **Точность ИИ-проверки** — соответствие оценок реальному качеству
- **Релевантность подсказок** — помогают ли подсказки решить задачу
- **Качество объяснений** — понятность ответов ИИ-помощника
- **Точность рекомендаций** — соответствие сложности уровню студента

---

## 🔮 Будущие улучшения

### Планируется добавить:

1. **Генерация тестов** — автоматическое создание unit-тестов для проверки кода
2. **Peer review** — возможность делиться решениями и получать фидбек от других студентов
3. **Персональные треки** — индивидуальные учебные планы на основе целей
4. **Интеграция с IDE** — расширения для VS Code / JetBrains
5. **Голосовой помощник** — объяснение теории голосом
6. **Видео-разборы** — генерация видео с объяснением решений
7. **Соревновательный режим** — челленджи и рейтинги
8. **Сертификация** — выдача сертификатов после прохождения курса

---

## 📝 Примеры промптов

### Проверка кода

```
Ты — опытный преподаватель программирования и code reviewer.

ЗАДАЧА УЧЕНИКА:
Название: Сумма двух чисел
Описание: Напишите функцию, которая принимает два числа и возвращает их сумму
Сложность: easy
Язык: python

КОД УЧЕНИКА:
```python
def sum(a, b):
    return a + b
```

ТВОЯ ЗАДАЧА:
Проанализируй код ученика и дай детальную обратную связь...
```

### Подсказка

```
Ты — опытный наставник по программированию. Ученик застрял на задаче и просит помощи.

ЗАДАЧА: Найти максимальное число в списке
ЯЗЫК: python
ПОПЫТКА: 3

КОД УЧЕНИКА:
```python
numbers = [1, 5, 3, 9, 2]
# не знаю, как найти максимум
```

ТВОЯ ЗАДАЧА:
Дай ученику подсказку, которая поможет продвинуться, но НЕ решай задачу полностью...
```

### Объяснение теории

```
Ты — опытный преподаватель программирования. Ученик изучает python и задал вопрос по теории.

КОНТЕКСТ ОБУЧЕНИЯ:
День: 15 из 90
Тема дня: Циклы for и while

ВОПРОС УЧЕНИКА:
В чём разница между for и while?

ТВОЯ ЗАДАЧА:
Дай понятное и подробное объяснение на вопрос ученика...
```

---

## 🤝 Вклад

При разработке новых ИИ-функций следуйте принципам:

1. **Образовательная ценность** — помогать учиться, а не давать готовые ответы
2. **Адаптивность** — учитывать уровень студента
3. **Конструктивность** — давать конкретные рекомендации
4. **Мотивация** — поддерживать и вдохновлять
5. **Прозрачность** — объяснять, почему даётся та или иная рекомендация

---

## 📞 Поддержка

При возникновении проблем с ИИ-функциями:

1. Проверьте наличие `HF_API_KEY` в `.env.local`
2. Убедитесь, что API-ключ валиден
3. Проверьте лимиты Hugging Face API
4. Изучите логи в консоли браузера и терминале

---

**Создано с ❤️ для VibeStudy**
