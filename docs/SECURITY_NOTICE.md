# 🔐 Security Notice - Telegram Bot Token

## ⚠️ ВАЖНО: Токен бота был скомпрометирован

Старый Telegram Bot Token был случайно закоммичен в репозиторий и должен быть заменён.

### Что делать:

#### 1. Создайте новый токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/mybots`
3. Выберите вашего бота **VibeStudy**
4. Нажмите **API Token** → **Revoke current token** (отозвать текущий)
5. Создайте новый токен через **Generate new token**
6. Скопируйте новый токен

#### 2. Обновите переменные окружения

**Локальная разработка:**
```bash
# Создайте файл .env.local в корне проекта
TELEGRAM_BOT_TOKEN=ваш_новый_токен_здесь
TELEGRAM_WEBHOOK_SECRET=сгенерируйте_секрет_через_openssl
```

**Production (Vercel):**
1. Откройте ваш проект на [Vercel Dashboard](https://vercel.com)
2. Settings → Environment Variables
3. Обновите `TELEGRAM_BOT_TOKEN` новым значением
4. Обновите `TELEGRAM_WEBHOOK_SECRET` (опционально, но рекомендуется)
5. Redeploy приложение

#### 3. Настройте webhook (для production)

```bash
# Запустите скрипт настройки webhook
npm run bot:webhook
```

### Генерация секрета для webhook

```bash
# Linux/Mac
openssl rand -base64 32

# Windows (PowerShell)
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))

# Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### История инцидента

- **Дата**: 2025-11-29
- **Файл**: `scripts/telegram-bot-local.js:6`
- **Токен**: `[REDACTED]` (отозван)
- **Исправление**: Токен удалён из кода, использование переменных окружения

### Меры предосторожности

✅ **Что сделано:**
- Токен удалён из исходного кода
- Добавлена проверка наличия токена перед запуском
- `.env.local` добавлен в `.gitignore`
- Создан `.env.example` с инструкциями

❌ **Что НЕЛЬЗЯ делать:**
- Не коммитьте `.env.local` в git
- Не публикуйте токены в issues/pull requests
- Не отправляйте токены в чатах/мессенджерах
- Не используйте один токен для dev и production

### Проверка безопасности

```bash
# Проверьте, что токен не закоммичен
git log --all --full-history --source --pretty=format:"%H %s" -- "*telegram*" | grep -i "token"

# Проверьте .gitignore
cat .gitignore | grep -E "\.env"
```

### Контакты

Если вы обнаружили уязвимость безопасности, пожалуйста, сообщите об этом конфиденциально.

---

**Последнее обновление**: 2025-11-29
