# 📱 Настройка Telegram бота для VibeStudy

## Обзор

Telegram бот для VibeStudy помогает студентам:
- 📅 Получать напоминания о занятиях
- 💡 Получать персональные советы на основе прогресса
- 📊 Просматривать статистику обучения
- 🔥 Поддерживать мотивацию и серии

---

## 🚀 Быстрый старт

### 1. Создание бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Укажите имя бота (например, "VibeStudy Bot")
4. Укажите username (например, "VibeStudyBot")
5. Сохраните полученный токен

### 2. Настройка переменных окружения

Добавьте в `.env.local`:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
CRON_SECRET=your_random_secret_key_here

# Уже существующие
HF_API_KEY=your_huggingface_key
```

**Генерация CRON_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 3. Настройка webhook

После деплоя на production, настройте webhook:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/api/telegram/webhook"}'
```

Проверка webhook:
```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

---

## 🎯 Функции бота

### Команды

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/help` | Справка по командам |
| `/stats` | Показать статистику обучения |
| `/advice` | Получить персональный совет |
| `/remind` | Настроить напоминания |

### Автоматические напоминания

Бот отправляет напоминания в выбранное время:
- 🌅 **Утро** (9:00)
- ☀️ **День** (14:00)
- 🌆 **Вечер** (19:00)
- 🌙 **Ночь** (22:00)

### Персональные советы

Бот анализирует:
- Текущий прогресс (день курса)
- Средний балл за задачи
- Серию дней подряд
- Слабые места в обучении
- Время с последней активности

---

## 🛠️ Архитектура

### Структура файлов

```
src/
├── telegram/
│   └── bot.ts                           # Логика бота
├── app/api/telegram/
│   ├── webhook/route.ts                 # Webhook для сообщений
│   └── send-reminder/route.ts           # Отправка напоминаний
├── lib/
│   └── telegram-db.ts                   # Работа с данными
├── components/profile/
│   └── TelegramSettings.tsx             # UI настроек
└── store/
    └── profile-store.ts                 # Хранение username

```

### Поток данных

```
Пользователь → Указывает username в профиле
                ↓
Бот → Отправляет /start
                ↓
Webhook → Сохраняет chatId
                ↓
Cron Job → Проверяет время напоминаний
                ↓
API → Отправляет сообщение через Bot API
```

---

## 📊 API Endpoints

### POST /api/telegram/webhook

Обрабатывает входящие сообщения от бота.

**Request:**
```json
{
  "update_id": 123456789,
  "message": {
    "message_id": 1,
    "from": {
      "id": 123456789,
      "username": "user123",
      "first_name": "John"
    },
    "chat": {
      "id": 123456789,
      "type": "private"
    },
    "text": "/start"
  }
}
```

**Response:**
```json
{
  "ok": true
}
```

### POST /api/telegram/send-reminder

Отправляет напоминания пользователям (вызывается по расписанию).

**Request:**
```json
{
  "secret": "your_cron_secret"
}
```

**Response:**
```json
{
  "success": true,
  "sent": 15,
  "errors": 0,
  "total": 15
}
```

---

## ⏰ Настройка Cron Jobs

### Vercel Cron (рекомендуется)

Создайте `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/telegram/send-reminder",
      "schedule": "0 9,14,19,22 * * *"
    }
  ]
}
```

### GitHub Actions

Создайте `.github/workflows/telegram-reminders.yml`:

```yaml
name: Telegram Reminders

on:
  schedule:
    - cron: '0 9,14,19,22 * * *'
  workflow_dispatch:

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Send reminders
        run: |
          curl -X POST https://your-domain.com/api/telegram/send-reminder \
            -H "Content-Type: application/json" \
            -d '{"secret": "${{ secrets.CRON_SECRET }}"}'
```

### Альтернатива: Cron-job.org

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org)
2. Создайте задачу:
   - URL: `https://your-domain.com/api/telegram/send-reminder`
   - Method: POST
   - Body: `{"secret": "your_cron_secret"}`
   - Schedule: `0 9,14,19,22 * * *`

---

## 💬 Примеры сообщений

### Напоминание о занятиях

```
🎯 Привет! Заметил, что ты не занимался уже 25 часов.

Ты на дне 15 из 90. Не теряй темп! 💪

Даже 15 минут практики сегодня помогут сохранить прогресс.
```

### Персональный совет

```
🎓 *Персональный совет на день 30*

⚠️ *Требуют внимания:*
• День 12: Циклы
• День 18: Функции
• День 25: Списки

Рекомендую повторить эти темы перед продолжением.

🎯 *Совет:*
Не спеши! Лучше хорошо усвоить основы, чем быстро пройти курс.

Удачи в обучении! 🚀
```

### Статистика

```
📊 *Твоя статистика VibeStudy*

🎯 Текущий день: 30/90
✅ Завершено: 25 дней (28%)
████████░░

🔥 Серия: 7 дней
⭐ Средний балл: 78/100
💻 Язык: PYTHON

Продолжай в том же духе! 🚀
```

---

## 🔒 Безопасность

### Защита webhook

1. **Проверка токена:** Telegram отправляет `X-Telegram-Bot-Api-Secret-Token` header
2. **Проверка IP:** Разрешить только IP Telegram серверов
3. **Rate limiting:** Ограничить количество запросов

### Защита cron endpoint

1. **Secret key:** Проверка `CRON_SECRET` в каждом запросе
2. **IP whitelist:** Разрешить только доверенные IP
3. **Логирование:** Записывать все попытки доступа

---

## 🧪 Тестирование

### Локальное тестирование

1. Используйте [ngrok](https://ngrok.com) для туннеля:
```bash
ngrok http 3000
```

2. Установите webhook на ngrok URL:
```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
  -d "url=https://your-ngrok-url.ngrok.io/api/telegram/webhook"
```

3. Отправьте команды боту в Telegram

### Тестирование команд

```bash
# Отправка тестового сообщения
curl -X POST "https://api.telegram.org/bot<TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": YOUR_CHAT_ID,
    "text": "Тестовое сообщение",
    "parse_mode": "Markdown"
  }'
```

---

## 📈 Мониторинг

### Логи

Проверяйте логи в консоли:
```bash
# Vercel
vercel logs

# Или в dashboard
https://vercel.com/your-project/logs
```

### Метрики

Отслеживайте:
- Количество отправленных сообщений
- Процент ошибок доставки
- Время отклика webhook
- Активные пользователи

---

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверьте webhook:
```bash
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

2. Проверьте логи сервера
3. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен

### Напоминания не приходят

1. Проверьте cron job (работает ли по расписанию)
2. Проверьте `CRON_SECRET`
3. Убедитесь, что username указан в профиле
4. Проверьте, что пользователь отправил `/start` боту

### Ошибка "Unauthorized"

1. Проверьте токен бота
2. Убедитесь, что бот не заблокирован пользователем
3. Проверьте права бота в @BotFather

---

## 🚀 Деплой

### Vercel (рекомендуется)

```bash
# Установите переменные окружения
vercel env add TELEGRAM_BOT_TOKEN
vercel env add CRON_SECRET

# Деплой
vercel --prod
```

### Другие платформы

Убедитесь, что платформа поддерживает:
- Webhook endpoints
- Cron jobs или scheduled functions
- Environment variables

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/BotFather)
- [Telegram Bot Examples](https://core.telegram.org/bots/samples)

---

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Изучите документацию Telegram Bot API
3. Проверьте переменные окружения
4. Убедитесь, что webhook настроен правильно

---

**Создано с ❤️ для VibeStudy**

