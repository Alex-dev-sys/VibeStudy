# 🚀 Загрузка проекта на GitHub

## 📋 Пошаговая инструкция

### Шаг 1: Создать репозиторий на GitHub

1. Откройте [github.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `VibeStudy` (или любое имя)
   - **Description**: `90-дневный курс программирования с AI и Telegram ботом`
   - **Public** или **Private** (на ваш выбор)
   - ⚠️ **НЕ** ставьте галочки на README, .gitignore, license
4. Нажмите **"Create repository"**

---

### Шаг 2: Команды в терминале

Откройте терминал в папке проекта и выполните:

```bash
# 1. Инициализировать Git (если ещё не сделано)
git init

# 2. Добавить все файлы
git add .

# 3. Сделать первый коммит
git commit -m "Initial commit: VibeStudy with AI, Supabase, and Telegram bot"

# 4. Переименовать ветку в main (если нужно)
git branch -M main

# 5. Добавить удалённый репозиторий
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 6. Загрузить на GitHub
git push -u origin main
```

---

### Шаг 3: Проверка

После выполнения команд:
1. Обновите страницу репозитория на GitHub
2. Вы увидите все файлы проекта! ✅

---

## ⚠️ ВАЖНО: Проверьте .gitignore

Убедитесь, что файл `.gitignore` содержит:

```
# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/
build/
dist/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
```

**Это важно!** Файл `.env.local` с вашими ключами НЕ должен попасть на GitHub!

---

## 🔑 Безопасность API ключей

### ✅ Что должно быть в `.gitignore`:
- `.env.local` ✅
- `.env` ✅
- `node_modules/` ✅

### ❌ Что НЕ должно попасть на GitHub:
- `HF_API_KEY` ❌
- `TELEGRAM_BOT_TOKEN` ❌
- `SUPABASE_SERVICE_ROLE_KEY` ❌
- Любые секретные ключи ❌

### ✅ Что безопасно публиковать:
- `NEXT_PUBLIC_SUPABASE_URL` ✅ (публичный)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` ✅ (защищён RLS)

---

## 🔄 Последующие обновления

После первой загрузки, для обновления проекта:

```bash
# 1. Добавить изменённые файлы
git add .

# 2. Сделать коммит с описанием изменений
git commit -m "Описание изменений"

# 3. Загрузить на GitHub
git push
```

---

## 📝 Примеры коммитов

```bash
# Добавление новой функции
git commit -m "feat: добавлена регистрация через Google"

# Исправление бага
git commit -m "fix: исправлена загрузка Monaco Editor"

# Обновление документации
git commit -m "docs: обновлён README с новыми функциями"

# Улучшение производительности
git commit -m "perf: оптимизирован Telegram бот"
```

---

## 🌿 Работа с ветками (опционально)

Если хотите работать с ветками:

```bash
# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на main
git checkout main

# Слить ветку в main
git merge feature/new-feature

# Удалить ветку
git branch -d feature/new-feature
```

---

## 🐛 Частые проблемы

### Ошибка: "remote origin already exists"

```bash
# Удалить старый origin
git remote remove origin

# Добавить новый
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### Ошибка: "failed to push some refs"

```bash
# Сначала получить изменения с GitHub
git pull origin main --allow-unrelated-histories

# Затем загрузить
git push -u origin main
```

### Проверить, какой origin установлен

```bash
git remote -v
```

---

## 📚 Создать README.md для GitHub

Файл `README.md` уже есть в проекте! Он автоматически отобразится на GitHub.

Если хотите обновить:

```bash
# Отредактируйте README.md
# Затем:
git add README.md
git commit -m "docs: обновлён README"
git push
```

---

## 🎉 Готово!

После загрузки на GitHub:
- ✅ Проект доступен по ссылке `https://github.com/YOUR_USERNAME/YOUR_REPO`
- ✅ Можно клонировать на другие устройства
- ✅ История всех изменений сохранена
- ✅ Можно работать в команде

---

## 🔗 Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- filename

# Посмотреть удалённые репозитории
git remote -v

# Обновить с GitHub
git pull
```

---

## 🚀 Следующий шаг: Deploy на Vercel

После загрузки на GitHub можно задеплоить на Vercel:

1. Откройте [vercel.com](https://vercel.com)
2. Нажмите **"Import Project"**
3. Выберите репозиторий с GitHub
4. Добавьте переменные окружения (API ключи)
5. Нажмите **"Deploy"**

Ваш проект будет доступен по ссылке! 🌐

