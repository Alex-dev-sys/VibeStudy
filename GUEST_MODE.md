# 🚀 Гостевой режим

## Что это?

**Гостевой режим** позволяет пользователям использовать VibeStudy **без регистрации**. Весь прогресс сохраняется в `localStorage` браузера.

---

## ✅ Что работает в гостевом режиме:

### **Полный доступ к обучению:**
- ✅ Все 90 дней курса
- ✅ Все языки программирования
- ✅ Генерация задач через AI
- ✅ Проверка кода через AI
- ✅ Подсказки и объяснения теории
- ✅ AI-помощник (чат)
- ✅ Playground (песочница для кода)
- ✅ Отслеживание прогресса (локально)
- ✅ Достижения
- ✅ История выполненных задач

### **Ограничения:**
- ⚠️ Прогресс сохраняется **только в браузере** (localStorage)
- ⚠️ При очистке кэша браузера прогресс **удалится**
- ⚠️ Прогресс **не синхронизируется** между устройствами
- ⚠️ Нет доступа к Telegram боту
- ⚠️ Нет облачного сохранения

---

## 🔧 Как это работает:

### **1. Вход без регистрации:**
На странице `/login` есть кнопка:
```
🚀 Продолжить без регистрации
```

При нажатии:
1. Устанавливается флаг `guestMode` в `localStorage`
2. Пользователь перенаправляется на `/learn`
3. Все функции работают, но без Supabase

### **2. Supabase становится опциональным:**

**До:**
```typescript
// Если нет ключей Supabase → ошибка и сайт ломается
if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error('Missing Supabase environment variables');
}
```

**После:**
```typescript
// Если нет ключей Supabase → работаем в гостевом режиме
if (!supabaseUrl || !supabaseAnonKey) {
  console.warn('⚠️ Supabase не настроен. Работа в гостевом режиме.');
}

export const supabase = (supabaseUrl && supabaseAnonKey) 
  ? createClient(supabaseUrl, supabaseAnonKey)
  : null;
```

### **3. Все функции проверяют наличие Supabase:**

```typescript
export async function getCurrentUser() {
  if (!supabase) {
    // В гостевом режиме возвращаем null
    return { user: null, error: null };
  }
  // ... обычная логика
}
```

---

## 📊 Хранение данных:

### **Гостевой режим (localStorage):**
- `vibestudy-progress` — прогресс обучения
- `vibestudy-profile` — профиль пользователя
- `vibestudy-achievements` — достижения
- `vibestudy-knowledge-profile` — профиль знаний
- `guestMode` — флаг гостевого режима

### **Зарегистрированные пользователи (Supabase):**
- Все данные синхронизируются с облаком
- Доступ с любого устройства
- Telegram бот для напоминаний

---

## 🎯 Преимущества:

### **Для пользователей:**
- ✅ Мгновенный старт без регистрации
- ✅ Нет необходимости вводить email
- ✅ Полный доступ ко всем функциям
- ✅ Можно зарегистрироваться позже

### **Для разработчиков:**
- ✅ Работает даже без настройки Supabase
- ✅ Легко тестировать локально
- ✅ Меньше зависимостей
- ✅ Упрощённый деплой

---

## 🔄 Переход на регистрацию:

Гость может в любой момент зарегистрироваться:
1. Перейти на `/login`
2. Войти через Google или Email
3. Прогресс из `localStorage` можно будет перенести в облако (TODO: функция миграции)

---

## 🚀 Deployment:

### **Без Supabase (только гостевой режим):**
Не нужно добавлять переменные Supabase на Vercel. Сайт будет работать в гостевом режиме.

### **С Supabase (полная функциональность):**
Добавьте на Vercel:
```
NEXT_PUBLIC_SUPABASE_URL=your_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
SUPABASE_SERVICE_ROLE_KEY=your_service_key
```

---

## ✅ Итог:

**Гостевой режим** делает VibeStudy доступным для всех, без необходимости регистрации. Это снижает порог входа и позволяет пользователям сразу начать обучение! 🎓

