# 🤖 Telegram Bot Implementation Guide

## Что реализовано

### ✅ Задача 1: База данных и инфраструктура
- SQL миграция с 6 таблицами
- TypeScript типы для всех сущностей
- Database утилиты для работы с данными
- Материализованное представление для аналитики
- RLS политики для безопасности

### ✅ Задача 2: Bot Controller и routing
- BotController класс для обработки сообщений
- Webhook endpoint для получения обновлений
- Утилиты для отправки сообщений с retry логикой
- Rate limiting (30 запросов/минуту)
- Обработка команд, callback queries, voice messages

### ✅ Задача 3: Базовые команды
- `/start` - приветствие с quick actions
- `/help` - список всех команд
- `/stats` - статистика обучения
- `/progress` - детальный прогресс
- `/topics` - мастерство по темам
- `/settings` - настройки бота

### 🚧 Задачи 4-20: В разработке
Остальные функции (AI сервис, аналитика, напоминания, геймификация и т.д.) имеют заглушки и готовы к реализации.

## Быстрый старт

### 1. Установка зависимостей

Все зависимости уже установлены в `package.json`.

### 2. Настройка переменных окружения

Добавь в `.env.local`:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_WEBHOOK_SECRET=random_secret_for_webhook_verification
CRON_SECRET=random_secret_for_cron_endpoints

# Supabase (уже должны быть)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Hugging Face (уже должен быть)
HF_TOKEN=your_huggingface_token
```

### 3. Запуск миграций базы данных

#### Вариант A: Через Supabase Dashboard
1. Открой SQL Editor в Supabase Dashboard
2. Скопируй содержимое `supabase/migrations/001_telegram_bot_tables.sql`
3. Выполни SQL

#### Вариант B: Через Supabase CLI
```bash
supabase db push
```

### 4. Создание Telegram бота

1. Найди [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Укажи имя: `VibeStudy Bot`
4. Укажи username: `VibeStudyBot` (или другой доступный)
5. Сохрани токен в `.env.local`

### 5. Локальное тестирование

```bash
# Запусти dev сервер
npm run dev

# В другом терминале, запусти ngrok для туннеля
ngrok http 3000

# Установи webhook (замени URL на свой ngrok URL)
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-ngrok-url.ngrok.io/api/telegram/webhook", "secret_token": "your_webhook_secret"}'
```

### 6. Тестирование бота

1. Найди своего бота в Telegram
2. Отправь `/start`
3. Попробуй другие команды: `/help`, `/stats`, `/settings`

## Деплой на Production

### 1. Деплой на Vercel

```bash
# Установи переменные окружения в Vercel
vercel env add TELEGRAM_BOT_TOKEN
vercel env add TELEGRAM_WEBHOOK_SECRET
vercel env add CRON_SECRET

# Деплой
vercel --prod
```

### 2. Настройка webhook

После деплоя, установи webhook на production URL:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-domain.vercel.app/api/telegram/webhook",
    "secret_token": "your_webhook_secret"
  }'
```

### 3. Проверка webhook

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

Должен вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://your-domain.vercel.app/api/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

## Структура проекта

```
src/
├── app/api/telegram/
│   └── webhook/
│       └── route.ts              # Webhook endpoint
├── lib/telegram/
│   ├── bot-controller.ts         # Главный контроллер
│   ├── database.ts               # Database утилиты
│   ├── send-message.ts           # Отправка сообщений
│   ├── callbacks.ts              # Обработка кнопок
│   ├── voice-handler.ts          # Голосовые сообщения
│   └── commands/
│       ├── index.ts              # Реестр команд
│       ├── start.ts              # /start
│       ├── help.ts               # /help
│       ├── stats.ts              # /stats
│       ├── progress.ts           # /progress
│       ├── topics.ts             # /topics
│       └── settings.ts           # /settings
├── types/
│   └── telegram.ts               # TypeScript типы
└── supabase/migrations/
    └── 001_telegram_bot_tables.sql  # SQL миграция
```

## Доступные команды

### Реализованные
- `/start` - Начать работу с ботом
- `/help` - Справка по командам
- `/stats` - Статистика обучения
- `/progress` - Детальный прогресс
- `/topics` - Мастерство по темам
- `/settings` - Настройки бота

### Заглушки (готовы к реализации)
- `/advice` - Персональный совет
- `/challenge` - Ежедневный челлендж
- `/remind` - Настроить напоминания
- `/plan` - План обучения
- `/predict` - Прогноз завершения
- `/community` - Сообщество
- `/language` - Сменить язык
- `/privacy` - Приватность
- `/hint` - Подсказка
- `/ask` - Задать вопрос AI
- `/schedule` - Запланировать сессию
- `/export` - Экспорт данных

## Следующие шаги

### Приоритет 1: AI сервис (Задача 4)
- Интеграция с Hugging Face для умных ответов
- Генерация рекомендаций и советов
- Система подсказок

### Приоритет 2: Аналитика (Задача 5)
- Анализ паттернов обучения
- Прогнозирование завершения курса
- Определение оптимального времени занятий

### Приоритет 3: Напоминания (Задача 6)
- Умное расписание напоминаний
- Адаптивная логика
- Защита streak

### Приоритет 4: Остальные функции
- Мотивационная система
- Геймификация
- Социальные функции
- Голосовые сообщения
- Мультиязычность

## Troubleshooting

### Бот не отвечает

1. Проверь webhook:
```bash
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

2. Проверь логи Vercel:
```bash
vercel logs
```

3. Проверь переменные окружения

### Ошибка "Unauthorized"

- Проверь `TELEGRAM_BOT_TOKEN`
- Убедись что бот не заблокирован пользователем

### Webhook не работает

- Проверь что URL доступен извне
- Проверь `TELEGRAM_WEBHOOK_SECRET`
- Убедись что webhook установлен правильно

## Мониторинг

### Логи

Все действия логируются в консоль. В production используй Vercel Logs:

```bash
vercel logs --follow
```

### Метрики

Отслеживай:
- Количество обработанных сообщений
- Время ответа
- Ошибки rate limiting
- Неизвестные команды

## Безопасность

✅ **Реализовано:**
- Webhook verification с secret token
- Rate limiting (30 req/min)
- Input sanitization
- RLS в базе данных

⚠️ **TODO:**
- IP whitelist для webhook
- Шифрование чувствительных данных
- Audit logging

## Поддержка

При возникновении проблем:
1. Проверь логи
2. Изучи документацию Telegram Bot API
3. Проверь переменные окружения
4. Убедись что миграции выполнены

---

**Статус:** Базовая функциональность реализована ✅  
**Версия:** 1.0  
**Дата:** 2025-11-14

