# 🗄️ Настройка Supabase для VibeStudy

## Что такое Supabase?

Supabase — это open-source альтернатива Firebase, предоставляющая:
- PostgreSQL базу данных
- Аутентификацию пользователей
- Real-time подписки
- Storage для файлов
- Row Level Security (RLS)

## 📋 Необходимые API ключи

Для работы с Supabase вам нужны **3 ключа**:

### 1. **NEXT_PUBLIC_SUPABASE_URL** 
URL вашего Supabase проекта
- Формат: `https://xxxxx.supabase.co`
- Где найти: Project Settings → API → Project URL

### 2. **NEXT_PUBLIC_SUPABASE_ANON_KEY**
Публичный ключ для клиентской части
- Формат: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
- Где найти: Project Settings → API → Project API keys → `anon` `public`
- ⚠️ Безопасен для использования в браузере (защищён RLS политиками)

### 3. **SUPABASE_SERVICE_ROLE_KEY** (опционально)
Серверный ключ с полным доступом
- Формат: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
- Где найти: Project Settings → API → Project API keys → `service_role` `secret`
- ⚠️ **НИКОГДА не используйте в клиентском коде!**
- Используется только для серверных операций (API routes)

---

## 🚀 Пошаговая настройка

### Шаг 1: Создать проект в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите "Start your project"
3. Войдите через GitHub
4. Нажмите "New Project"
5. Заполните:
   - **Name**: `vibestudy` (или любое имя)
   - **Database Password**: придумайте надёжный пароль (сохраните его!)
   - **Region**: выберите ближайший регион (например, `Europe West (London)`)
6. Нажмите "Create new project"
7. Подождите 2-3 минуты пока проект создаётся

### Шаг 2: Получить API ключи

1. В левом меню выберите ⚙️ **Settings**
2. Перейдите в **API**
3. Скопируйте:
   - **Project URL** → это ваш `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** → это ваш `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role secret** → это ваш `SUPABASE_SERVICE_ROLE_KEY`

### Шаг 3: Добавить ключи в проект

Откройте файл `.env.local` в корне проекта и добавьте:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_ключ
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_ключ
```

**Пример:**

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://qtswuibugwuvgzppkbtq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF0c3d1aWJ1Z3d1dmd6cHBrYnRxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjI2MTc0NjcsImV4cCI6MjA3ODE5MzQ2N30.elUp5IX7YHKJQPQa5SFzhqQVgwZHBfvPw4BuYoIHS6A
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF0c3d1aWJ1Z3d1dmd6cHBrYnRxIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MjYxNzQ2NywiZXhwIjoyMDc4MTkzNDY3fQ.q3JGkniUcjC4MK6Hm7-qdkSWhcJvj9c1zGSbzvSI81Y
```

### Шаг 4: Создать таблицы в базе данных

1. В Supabase Dashboard перейдите в **SQL Editor** (иконка 📊)
2. Нажмите "+ New query"
3. Скопируйте содержимое файла `supabase/schema.sql`
4. Вставьте в редактор
5. Нажмите **Run** (или Ctrl/Cmd + Enter)
6. Убедитесь, что все таблицы созданы успешно

Или выполните через терминал:

```bash
# Установите Supabase CLI (если ещё не установлен)
npm install -g supabase

# Войдите в Supabase
supabase login

# Примените миграцию
supabase db push
```

### Шаг 5: Проверить таблицы

1. Перейдите в **Table Editor** (иконка 📋)
2. Вы должны увидеть таблицы:
   - `users` — пользователи
   - `user_progress` — прогресс по темам
   - `task_attempts` — попытки решения задач
   - `user_achievements` — достижения
   - `topic_mastery` — мастерство по темам
   - `generated_content_cache` — кэш AI контента

### Шаг 6: Настроить Row Level Security (RLS)

RLS уже настроен в `schema.sql`, но проверьте:

1. Перейдите в **Authentication** → **Policies**
2. Для каждой таблицы должны быть политики:
   - `Users can view own data`
   - `Users can insert own data`
   - `Users can update own data`

Это гарантирует, что пользователи видят только свои данные.

### Шаг 7: (Опционально) Настроить аутентификацию

Если хотите добавить регистрацию/вход:

1. Перейдите в **Authentication** → **Providers**
2. Включите нужные провайдеры:
   - Email
   - Google
   - GitHub
   - и т.д.

---

## 🧪 Тестирование подключения

Перезапустите dev сервер:

```bash
npm run dev
```

Откройте консоль браузера (F12) и проверьте:
- Нет ошибок подключения к Supabase
- Данные сохраняются в базу данных

---

## 📊 Что хранится в базе данных?

### 1. **users** — Пользователи
```sql
id, username, email, created_at, updated_at
```

### 2. **user_progress** — Прогресс по темам
```sql
user_id, topic_id, completed, score, time_spent, last_accessed
```

### 3. **task_attempts** — Попытки решения задач
```sql
user_id, task_id, code, result, is_correct, hints_used, time_spent
```

### 4. **user_achievements** — Достижения
```sql
user_id, achievement_id, unlocked_at
```

### 5. **topic_mastery** — Мастерство по темам (для адаптивного обучения)
```sql
user_id, topic, mastery_level, total_attempts, successful_attempts, last_practice
```

### 6. **generated_content_cache** — Кэш AI контента
```sql
content_type, topic, difficulty, language, content, expires_at
```

---

## 🔒 Безопасность

### ✅ Безопасно использовать в браузере:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

Эти ключи защищены Row Level Security (RLS) политиками.

### ⚠️ НИКОГДА не используйте в браузере:
- `SUPABASE_SERVICE_ROLE_KEY`

Этот ключ даёт полный доступ к базе данных и должен использоваться только в серверном коде (API routes).

---

## 🐛 Частые проблемы

### Ошибка: "Missing Supabase environment variables"

**Решение:**
1. Проверьте, что файл `.env.local` существует
2. Убедитесь, что все 3 ключа добавлены
3. Перезапустите dev сервер: `npm run dev`

### Ошибка: "Invalid API key"

**Решение:**
1. Проверьте, что скопировали правильные ключи
2. Убедитесь, что нет лишних пробелов
3. Попробуйте создать новые ключи в Supabase Dashboard

### Ошибка: "Row Level Security policy violation"

**Решение:**
1. Убедитесь, что RLS политики созданы (см. Шаг 6)
2. Проверьте, что пользователь аутентифицирован
3. Проверьте, что `user_id` совпадает с `auth.uid()`

### Данные не сохраняются

**Решение:**
1. Откройте консоль браузера (F12) и проверьте ошибки
2. Перейдите в Supabase Dashboard → Table Editor и проверьте таблицы
3. Проверьте, что RLS политики настроены правильно

---

## 📚 Полезные ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase JavaScript Client](https://supabase.com/docs/reference/javascript/introduction)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [Supabase CLI](https://supabase.com/docs/guides/cli)

---

## 🎉 Готово!

Теперь ваш прогресс сохраняется в облачной базе данных Supabase! 🚀

Все данные синхронизируются автоматически:
- ✅ Прогресс по темам
- ✅ Попытки решения задач
- ✅ Достижения
- ✅ Мастерство по темам
- ✅ Кэш AI контента

**Следующий шаг:** Добавьте аутентификацию пользователей для полноценной работы!

