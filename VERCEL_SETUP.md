# 🚀 Настройка Environment Variables на Vercel

## ⚠️ Обязательные переменные для работы проекта

### 📋 Список всех переменных:

#### 1. **Supabase (обязательно для аутентификации)**

```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
```

**Где взять:**
- Откройте [Supabase Dashboard](https://supabase.com/dashboard)
- Выберите ваш проект
- Settings → API
- Скопируйте: Project URL, anon public key, service_role key

#### 2. **Hugging Face API (обязательно для AI функций)**

```
HF_TOKEN=hf_your_access_token
# Необязательно
# HF_API_BASE_URL=https://router.huggingface.co/v1
# HF_MODEL=MiniMaxAI/MiniMax-M2:novita
```

**Где взять:**
- Откройте [Hugging Face Settings](https://huggingface.co/settings/tokens)
- Создайте токен с правами `read`
- Скопируйте ключ и добавьте его как `HF_TOKEN`
- (Опционально) укажите собственную модель через `HF_MODEL`

#### 3. **Telegram Bot (опционально)**

```
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
CRON_SECRET=your_random_secret_string
```

**Где взять:**
- Telegram Bot Token: создайте бота через [@BotFather](https://t.me/BotFather)
- CRON_SECRET: любая случайная строка (для безопасности)

---

## 🔧 Как добавить на Vercel:

### Способ 1: При первом деплое

1. Откройте [vercel.com](https://vercel.com)
2. Нажмите **"Add New"** → **"Project"**
3. Выберите репозиторий **Alex-dev-sys/VibeStudy**
4. Нажмите **"Import"**
5. В секции **"Environment Variables"** добавьте все переменные:
   - Нажмите **"Add"**
   - Введите **Name** (например, `NEXT_PUBLIC_SUPABASE_URL`)
   - Введите **Value** (скопируйте из списка выше)
   - Выберите **"Production"**, **"Preview"**, **"Development"** (все 3)
   - Нажмите **"Add"**
6. Повторите для всех переменных
7. Нажмите **"Deploy"**

### Способ 2: После деплоя

1. Откройте ваш проект на Vercel
2. Перейдите в **"Settings"**
3. Выберите **"Environment Variables"**
4. Для каждой переменной:
   - Нажмите **"Add New"**
   - Введите **Key** и **Value**
   - Выберите окружения (Production, Preview, Development)
   - Нажмите **"Save"**
5. После добавления всех переменных:
   - Перейдите в **"Deployments"**
   - Нажмите **"..."** на последнем деплое
   - Выберите **"Redeploy"**

---

## ✅ Проверка переменных

После добавления переменных проверьте:

1. Откройте ваш сайт на Vercel (например, `your-project.vercel.app`)
2. Перейдите на `/login`
3. Попробуйте войти через Google или Email
4. Если всё работает - переменные настроены правильно! ✅

---

## 🐛 Если что-то не работает:

### Ошибка: "Missing Supabase environment variables"

**Решение:**
1. Убедитесь, что добавлены **ВСЕ 3** переменные Supabase:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
2. Проверьте, что нет опечаток в названиях
3. Проверьте, что значения скопированы полностью
4. Сделайте **Redeploy**

### Ошибка при входе через Google

**Решение:**
1. Проверьте, что Google OAuth настроен в Supabase
2. Убедитесь, что redirect URI правильный
3. Проверьте консоль браузера (F12) на ошибки

### AI функции не работают

**Решение:**
1. Убедитесь, что добавлена переменная `HF_TOKEN`
2. Проверьте, что ключ действителен
3. Сделайте **Redeploy**

---

## 📊 Таблица переменных

| Переменная | Обязательна? | Для чего |
|------------|--------------|----------|
| `NEXT_PUBLIC_SUPABASE_URL` | ✅ Да | URL Supabase проекта |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✅ Да | Публичный ключ Supabase |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ Да | Серверный ключ Supabase |
| `HF_TOKEN` | ✅ Да | Hugging Face токен доступа |
| `HF_API_BASE_URL` | ⚙️ Нет | Переопределение базового URL Hugging Face Router |
| `HF_MODEL` | ⚙️ Нет | Явный выбор модели Hugging Face |
| `TELEGRAM_BOT_TOKEN` | ⚙️ Нет | Токен Telegram бота |
| `CRON_SECRET` | ⚙️ Нет | Секрет для cron задач |

---

## 🔒 Безопасность

### ✅ Безопасно публиковать:
- `NEXT_PUBLIC_SUPABASE_URL` - публичный URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - защищён RLS

### ⚠️ НИКОГДА не публикуйте:
- `HF_TOKEN` - секретный ключ Hugging Face
- `SUPABASE_SERVICE_ROLE_KEY` - полный доступ к БД
- `TELEGRAM_BOT_TOKEN` - токен бота
- `CRON_SECRET` - секрет

**Эти ключи должны быть только в Environment Variables на Vercel!**

---

## 🎉 Готово!

После добавления всех переменных и redeploy ваш проект будет работать полностью! 🚀

**Ссылка на проект:** `https://your-project.vercel.app`

