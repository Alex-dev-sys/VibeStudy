# VibeStudy Environment Variables Template
# Copy this file to .env.local and fill in your values
# NEVER commit .env files with real secrets to Git!

# =============================================================================
# SECURITY SETTINGS (REQUIRED IN PRODUCTION)
# =============================================================================

# CSRF Protection Secret - generate with: openssl rand -hex 32
CSRF_SECRET=your-csrf-secret-here-generate-with-openssl-rand-hex-32

# Admin API Secret - generate with: openssl rand -hex 32
# Used for admin endpoints like cache clearing
ADMIN_SECRET=your-admin-secret-here-generate-with-openssl-rand-hex-32

# Optional: Admin IP Whitelist (comma-separated IPs)
# If set, admin endpoints will only accept requests from these IPs
# ADMIN_ALLOWED_IPS=192.168.1.100,10.0.0.1

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Public Supabase URL and anon key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (NEVER expose to client - server-side only)
# Only use for operations that need to bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# TELEGRAM BOT CONFIGURATION
# =============================================================================

# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Webhook secret for validating incoming updates
# Generate with: openssl rand -hex 32
# Set this same value when calling setWebhook API
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret-here

# Webhook URL (your public domain)
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/api/webhook

# =============================================================================
# AI API CONFIGURATION
# =============================================================================

# AI API Token (GPT Lama or other OpenAI-compatible API)
AI_API_TOKEN=your-ai-api-token-here
AI_API_BASE_URL=https://api.gptlama.ru/v1

# Model configuration
AI_MODEL_FREE=gemini-1.5-flash
AI_MODEL_PREMIUM=gpt-4o
AI_MODEL_PRO=claude-3-5-sonnet

# =============================================================================
# TON BLOCKCHAIN CONFIGURATION
# =============================================================================

# Your receiving wallet address
TON_WALLET_ADDRESS=EQC...your-wallet-address

# TON API for transaction verification
TON_API_KEY=your-ton-api-key

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Public app URL
NEXT_PUBLIC_APP_URL=https://yourdomain.com
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Environment
NODE_ENV=production

# Logging level: error, warn, info, debug
LOG_LEVEL=info

# =============================================================================
# OPTIONAL: ERROR TRACKING
# =============================================================================

# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn

# Error tracking webhook URL
# ERROR_TRACKING_WEBHOOK=https://your-webhook-url

# =============================================================================
# SECURITY BEST PRACTICES
# =============================================================================
#
# 1. Generate all secrets using cryptographically secure random:
#    openssl rand -hex 32
#
# 2. Never commit .env files to Git
#
# 3. Use different secrets for development and production
#
# 4. Rotate secrets periodically (especially after security incidents)
#
# 5. Use environment-specific files:
#    - .env.local (local development)
#    - .env.production (production server)
#
# 6. On your VPS, set these as system environment variables or use
#    a secrets manager like Doppler, Vault, or AWS Secrets Manager
#
