# ⚡ Быстрый старт Supabase

## ✅ Ключи уже добавлены!

Ваши ключи Supabase уже настроены в `.env.local`:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://qtswuibugwuvgzppkbtq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 🗄️ Осталось только создать таблицы!

### Вариант 1: Через Supabase Dashboard (рекомендуется)

1. Откройте [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. Выберите проект **qtswuibugwuvgzppkbtq**
3. Перейдите в **SQL Editor** 📊 (в левом меню)
4. Нажмите **"+ New query"**
5. Скопируйте **весь** файл `supabase/schema.sql`
6. Вставьте в редактор
7. Нажмите **Run** (или Ctrl/Cmd + Enter)
8. Дождитесь сообщения "Success. No rows returned"

### Вариант 2: Через Supabase CLI

```bash
# Установите CLI (если ещё не установлен)
npm install -g supabase

# Войдите в Supabase
supabase login

# Подключитесь к проекту
supabase link --project-ref qtswuibugwuvgzppkbtq

# Примените схему
supabase db push
```

---

## 🎯 Проверьте, что таблицы созданы

1. Откройте **Table Editor** 📋 в Supabase Dashboard
2. Вы должны увидеть 6 таблиц:
   - ✅ `users` — пользователи
   - ✅ `user_progress` — прогресс по темам
   - ✅ `task_attempts` — попытки решения задач
   - ✅ `user_achievements` — достижения
   - ✅ `topic_mastery` — мастерство по темам
   - ✅ `generated_content_cache` — кэш AI контента

---

## 🚀 Запустите проект

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

---

## ✅ Что теперь работает:

- ✅ **Прогресс сохраняется в облаке** (не только в браузере)
- ✅ **Синхронизация между устройствами**
- ✅ **История всех попыток решения задач**
- ✅ **Достижения хранятся навсегда**
- ✅ **Адаптивное обучение** на основе ваших ошибок
- ✅ **Кэш AI контента** (быстрее и дешевле)

---

## 🐛 Если что-то не работает

### Ошибка: "Missing Supabase environment variables"

**Решение:** Перезапустите dev сервер:
```bash
# Остановите сервер (Ctrl+C)
npm run dev
```

### Ошибка: "relation does not exist"

**Решение:** Таблицы не созданы. Выполните SQL из `supabase/schema.sql` (см. выше).

### Ошибка: "Invalid API key"

**Решение:** Проверьте, что ключи скопированы полностью (без пробелов в начале/конце).

---

## 📚 Подробная документация

Смотрите **`SUPABASE_SETUP.md`** для полной инструкции!

---

## 🎉 Готово!

После создания таблиц всё будет работать автоматически! 🚀

