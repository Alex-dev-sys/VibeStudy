# Руководство по генерации учебного контента

## Обзор новой системы

Мы создали улучшенную систему для генерации и управления учебным контентом с автоматическими тестами и адаптивным обучением.

## Что нового?

### 1. Расширенная структура контента (`EnhancedDayContent`)

Каждый день теперь включает:
- ✅ Подробная теория с примерами
- ✅ Вопросы для самопроверки (quiz + текстовые)
- ✅ 5 задач с автоматическими тестами
- ✅ Starter code для помощи студентам
- ✅ Reference solution для проверки
- ✅ Метаданные (сложность, категория, время)
- ✅ Связи с другими темами
- ✅ Ресурсы для дополнительного изучения

### 2. Автоматическая проверка задач

```typescript
interface TestCase {
  input: string | string[];
  expected: any;
  description: string;
  hidden?: boolean; // Скрытые тесты
}
```

Каждая задача теперь имеет тесты, которые автоматически проверяют решение студента!

### 3. Адаптивное обучение

Система анализирует:
- Скорость выполнения задач
- Количество попыток
- Использование подсказок
- Слабые и сильные темы

И предоставляет персонализированные рекомендации!

---

## Как использовать

### Генерация контента

#### Базовая генерация (шаблон)

```bash
# Сгенерировать дни 3-10 для Python
npm run generate:curriculum -- --language=python --days=3-10

# Сгенерировать дни 1-30 для JavaScript
npm run generate:curriculum -- --language=javascript --days=1-30

# Сгенерировать все 90 дней
npm run generate:curriculum -- --language=python --days=all
```

#### С использованием AI (coming soon)

```bash
npm run generate:curriculum -- --language=python --days=3-10 --ai
```

### Структура сгенерированных файлов

```
generated-curriculum/
├── curriculum-python-days-3-10.json
├── curriculum-python-days-11-20.json
├── curriculum-javascript-days-3-10.json
└── ...
```

### Использование в приложении

```typescript
import { ENHANCED_CURRICULUM_SAMPLE } from '@/data/enhanced-curriculum-sample';
import { executeCodeWithTests } from '@/lib/testing/code-validator';

// Получить контент для дня
const dayContent = ENHANCED_CURRICULUM_SAMPLE[3];

// Проверить решение студента
const code = `...`; // код студента
const task = dayContent.tasks[0];

const result = await executeCodeWithTests(
  code,
  task.tests!,
  'python'
);

console.log(result.score); // 0-100
console.log(result.feedback); // Feedback для студента
```

### Адаптивное обучение

```typescript
import {
  analyzeLearningProfile,
  generateRecommendations,
  getAdaptiveTasks
} from '@/lib/learning/adaptive-system';

// Анализ профиля студента
const profile = analyzeLearningProfile(userId, taskAttempts, currentDay);

// Получение рекомендаций
const recommendations = generateRecommendations(profile, currentDayTasks);

// Адаптация задач под уровень
const adaptedTasks = getAdaptiveTasks(profile, allTasks);
```

---

## Создание контента вручную

### Пример полного дня

См. `src/data/enhanced-curriculum-sample.ts` для примера дня 3.

### Шаблон задачи с тестами

```typescript
{
  id: 'day3-task1',
  difficulty: 'easy',
  prompt: 'Описание задачи...',
  solutionHint: 'Подсказка...',

  // Автоматические тесты
  tests: [
    {
      input: '5',
      expected: 'положительное',
      description: 'Положительное число'
    },
    {
      input: '-3',
      expected: 'отрицательное',
      description: 'Отрицательное число'
    },
    {
      input: '0',
      expected: 'ноль',
      description: 'Ноль',
      hidden: true // Скрытый тест
    }
  ],

  // Код для старта
  starterCode: `# Начальный код
number = int(input("Введите число: "))

# Ваш код здесь
`,

  // Эталонное решение (не показывается студенту)
  referenceSolution: `number = int(input())

if number > 0:
    print("положительное")
elif number < 0:
    print("отрицательное")
else:
    print("ноль")`,

  concepts: ['условия', 'if-else'],
  estimatedMinutes: 10
}
```

---

## Процесс создания контента для 90 дней

### Этап 1: Подготовка (Дни 1-2)

- [x] Создать новую структуру типов
- [x] Создать систему тестирования
- [x] Создать систему адаптивного обучения
- [x] Создать генератор контента

### Этап 2: Базовый контент (Дни 3-30)

**Python:**
- [ ] Дни 3-15: Основы (переменные, условия, циклы)
- [ ] Дни 16-25: Функции и структуры данных
- [ ] Дни 26-30: Файлы и обработка ошибок

**JavaScript:**
- [ ] Дни 3-15: Основы (переменные, условия, циклы)
- [ ] Дни 16-22: Массивы и методы
- [ ] Дни 23-30: DOM и события

### Этап 3: Продвинутый контент (Дни 31-60)

**Python:**
- [ ] Дни 31-42: ООП
- [ ] Дни 43-55: NumPy, Pandas
- [ ] Дни 56-60: Web и БД

**JavaScript:**
- [ ] Дни 31-42: Асинхронность и ООП
- [ ] Дни 43-50: Продвинутые концепции
- [ ] Дни 51-60: React

### Этап 4: Специализация (Дни 61-90)

**Python:**
- [ ] Дни 61-72: FastAPI
- [ ] Дни 73-82: Machine Learning
- [ ] Дни 83-90: DevOps

**JavaScript:**
- [ ] Дни 61-70: Next.js
- [ ] Дни 71-80: Node.js и БД
- [ ] Дни 81-90: Full-stack проект

---

## Best Practices

### Написание теории

1. **Начни с простого объяснения**
   - Используй аналогии из жизни
   - Объясни, ЗАЧЕМ это нужно

2. **Приведи примеры**
   - Минимум 2-3 примера
   - От простого к сложному

3. **Добавь практические советы**
   - Типичные ошибки
   - Best practices

### Создание задач

1. **Прогрессия сложности**
   - Easy: 2 задачи (закрепление базы)
   - Medium: 2 задачи (применение)
   - Hard: 1 задача (творческое применение)

2. **Хорошие тесты**
   - Минимум 3 теста на задачу
   - Покрывают граничные случаи
   - Один скрытый тест

3. **Starter code**
   - Помогает студентам начать
   - Не дает готового решения
   - Содержит комментарии-подсказки

---

## Метрики качества

Каждый день должен соответствовать:

- [ ] Теория: 300-500 слов
- [ ] Примеры: 2-3 шт
- [ ] Задачи: 5 шт
- [ ] Тесты: минимум 3 на задачу
- [ ] Время: 45-90 минут на полное прохождение
- [ ] Концепции: связаны с предыдущими днями

---

## Автоматизация

### Массовая генерация

```bash
# Скрипт для генерации всего Python курса
for i in {3..90}; do
  npm run generate:curriculum -- --language=python --days=$i-$i
  sleep 2
done
```

### Проверка качества

```bash
# TODO: Создать скрипт валидации
npm run validate:curriculum
```

---

## Roadmap

### Ближайшие улучшения

- [ ] Интеграция с реальным AI API для генерации
- [ ] Автоматическая валидация тестов
- [ ] Peer review система для контента
- [ ] A/B тестирование разных версий уроков
- [ ] Видео-примеры для сложных тем
- [ ] Интерактивные визуализации

### Долгосрочные планы

- [ ] Поддержка всех 7 языков
- [ ] Персонализированные треки обучения
- [ ] Community-contributed контент
- [ ] Переводы на другие языки
- [ ] Интеграция с IDE

---

## Контакты и поддержка

Вопросы по генерации контента:
- Создай issue в репозитории
- Обратись к команде контента

**Happy teaching! 🚀**
