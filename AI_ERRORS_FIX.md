# Исправление ошибок AI генерации

## Проблемы, которые были исправлены

### 1. Таймауты и обрывы соединения
**Проблема:** `TypeError: terminated` и `SocketError: other side closed`
- AI запросы обрывались из-за короткого таймаута (60 секунд)
- Нет обработки сетевых ошибок (ECONNRESET, socket hang up)

**Решение:**
- ✅ Увеличен таймаут до 90 секунд для fetch запросов
- ✅ Увеличен таймаут AI очереди до 120 секунд (2 минуты)
- ✅ Добавлен AbortController для корректной отмены запросов
- ✅ Добавлена обработка сетевых ошибок с понятными сообщениями
- ✅ Добавлен заголовок `Connection: keep-alive` для стабильности соединения

### 2. Отсутствие retry логики
**Проблема:** При сбое запроса сразу возвращался fallback контент

**Решение:**
- ✅ Добавлена retry логика с экспоненциальной задержкой (2s, 4s, 6s)
- ✅ Различение сетевых ошибок от других типов ошибок
- ✅ Максимум 2 попытки перед использованием fallback
- ✅ Логирование каждой попытки для отладки

### 3. Проблемы с сохранением контента
**Проблема:** 404 ошибки при запросе `/api/get-content`

**Решение:**
- ✅ Исправлен вызов `saveGeneratedContent` - добавлен `await`
- ✅ Добавлено логирование успешного сохранения
- ✅ Улучшена обработка ошибок при сохранении
- ✅ Добавлено предупреждение при пропуске сохранения fallback контента

### 4. Недостаточное логирование
**Проблема:** Сложно отследить, где именно происходит сбой

**Решение:**
- ✅ Добавлено детальное логирование в AI очереди
- ✅ Логирование типа ошибки и метаданных
- ✅ Логирование каждой попытки retry
- ✅ Логирование успешного/неуспешного сохранения

## Изменённые файлы

1. **src/lib/ai-client.ts**
   - Добавлен AbortController с таймаутом 90 секунд
   - Добавлена обработка сетевых ошибок (ECONNRESET, AbortError)
   - Добавлен заголовок Connection: keep-alive
   - Улучшена обработка ошибок в fallback запросах

2. **src/lib/ai/pipeline.ts**
   - Увеличен DEFAULT_TIMEOUT до 120 секунд
   - Добавлено детальное логирование ошибок с типом и метаданными

3. **src/app/api/generate-tasks/route.ts**
   - Увеличен таймаут до 120 секунд
   - Увеличен maxTokens до 2000
   - Добавлена retry логика с экспоненциальной задержкой
   - Различение сетевых ошибок
   - Исправлен вызов saveGeneratedContent (добавлен await)
   - Добавлено логирование сохранения контента

## Как проверить исправления

1. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

2. Откройте приложение и попробуйте сгенерировать контент для дня 1

3. Проверьте логи в консоли:
   - Должны видеть попытки генерации
   - При сетевых ошибках - retry с задержкой
   - Успешное сохранение контента

4. Проверьте, что контент сохраняется:
   - Файл `data/generated-content.json` должен содержать данные
   - Повторный запрос к `/api/get-content` должен возвращать 200, а не 404

## Дополнительные рекомендации

### Если проблемы продолжаются:

1. **Проверьте AI_API_TOKEN:**
   ```bash
   echo %AI_API_TOKEN%
   ```
   Должен быть валидный токен от GPT Llama

2. **Проверьте доступность API:**
   ```bash
   curl -H "Authorization: Bearer YOUR_TOKEN" https://api.gptlama.ru/v1/models
   ```

3. **Увеличьте таймауты ещё больше** (если интернет медленный):
   - В `ai-client.ts`: увеличьте с 90000 до 120000
   - В `generate-tasks/route.ts`: увеличьте с 120_000 до 180_000

4. **Проверьте файловую систему:**
   - Убедитесь, что папка `data/` существует
   - Проверьте права на запись

## Мониторинг

Следите за этими логами:
- `✅ Content saved successfully` - контент сохранён
- `⚠️ Using fallback after all retries failed` - все попытки провалились
- `🔍 Raw API Response` - ответ от AI API
- `AI job completed` - задача в очереди завершена
