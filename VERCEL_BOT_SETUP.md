# 🚀 Настройка Telegram бота на Vercel

Полная инструкция по настройке автоматической работы Telegram бота вместе с сайтом на Vercel.

---

## 🎯 Как это работает

**На Vercel бот работает через Webhook:**
- Сайт деплоится на Vercel
- Telegram отправляет все сообщения на ваш webhook endpoint: `https://ваш-домен.vercel.app/api/telegram/webhook`
- Next.js API route обрабатывает сообщения
- Бот работает **автоматически 24/7** без дополнительных серверов!

**Преимущества:**
- ✅ Бот работает всегда вместе с сайтом
- ✅ Не нужен отдельный сервер
- ✅ Автоматическое масштабирование
- ✅ Бесплатный план Vercel подходит

---

## 📋 Шаг 1: Подготовка

### 1.1 Создай нового бота (если ещё не создал)

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Отправь команду `/newbot`
3. Следуй инструкциям:
   - Введи имя бота (например: `VibeStudy Bot`)
   - Введи username бота (должен заканчиваться на `bot`, например: `VibeStudyBot`)
4. **Сохрани токен** который выдаст BotFather (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 1.2 Создай webhook secret

Это секретный ключ для безопасности webhook:

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
```

**Linux/Mac:**
```bash
openssl rand -base64 32
```

**Node.js:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

Сохрани полученную строку.

---

## 📦 Шаг 2: Настройка Vercel

### 2.1 Залей проект на Vercel

1. Зайди на [vercel.com](https://vercel.com)
2. Подключи свой GitHub репозиторий
3. Импортируй проект VibeStudy

### 2.2 Добавь переменные окружения

В Vercel Dashboard → Settings → Environment Variables добавь:

```bash
# ОБЯЗАТЕЛЬНЫЕ переменные для бота
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_WEBHOOK_SECRET=твой_секрет_из_шага_1.2
NEXT_PUBLIC_SITE_URL=https://твой-домен.vercel.app

# Остальные переменные из .env.example
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
AI_API_TOKEN=...
# и т.д.
```

**ВАЖНО:**
- Добавь переменные для **всех окружений** (Production, Preview, Development)
- После добавления нажми **Save**
- **Redeploy** проект чтобы изменения применились

### 2.3 Получи URL проекта

После деплоя Vercel выдаст URL вида:
- Production: `https://твой-проект.vercel.app`
- Или кастомный домен: `https://vibestudy.com`

Сохрани этот URL.

---

## 🔧 Шаг 3: Настройка Webhook

### Вариант А: Локальная настройка (рекомендуется)

1. **Клонируй проект локально** (если ещё не сделал):
```bash
git clone https://github.com/твой-username/VibeStudy
cd VibeStudy
npm install
```

2. **Создай `.env.local`** с токеном:
```bash
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_WEBHOOK_SECRET=твой_секрет
NEXT_PUBLIC_SITE_URL=https://твой-проект.vercel.app
```

3. **Запусти автонастройку:**
```bash
node scripts/setup-telegram-webhook.js auto
```

Скрипт автоматически:
- ✅ Удалит старый webhook (если был)
- ✅ Установит новый webhook на Vercel
- ✅ Настроит menu button
- ✅ Проверит подключение

### Вариант Б: Ручная настройка через curl

**Установи webhook:**
```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://твой-проект.vercel.app/api/telegram/webhook",
    "secret_token": "ваш_webhook_secret",
    "allowed_updates": ["message", "callback_query"]
  }'
```

**Проверь webhook:**
```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверь статус webhook

```bash
node scripts/setup-telegram-webhook.js check
```

Должен показать:
```
✓ URL: https://твой-проект.vercel.app/api/telegram/webhook
✓ Pending updates: 0
✓ Last error: нет
```

### 4.2 Проверь health endpoint

Открой в браузере:
```
https://твой-проект.vercel.app/api/telegram/health
```

Должен показать статус:
```json
{
  "status": "healthy",
  "webhook": {
    "totalUpdates": 0,
    "successRate": "100%"
  }
}
```

### 4.3 Протестируй бота

1. Открой бота в Telegram: `https://t.me/твой_бот`
2. Отправь `/start`
3. Проверь команды:
   - `/stats` - статистика
   - `/help` - помощь
   - `/run print("Hello")` - запуск кода

**Если бот не отвечает:**
- Проверь логи в Vercel Dashboard → Deployment → Functions
- Проверь переменные окружения
- Запусти `node scripts/setup-telegram-webhook.js check`

---

## 🔄 Шаг 5: Обновление webhook (при изменении домена)

Если изменил домен или переезжаешь на другой URL:

```bash
# Автоматически
node scripts/setup-telegram-webhook.js auto

# Или вручную
node scripts/setup-telegram-webhook.js set https://новый-домен.vercel.app/api/telegram/webhook
```

---

## 🎨 Дополнительно: Mini App в меню

Настрой кнопку Mini App в меню бота:

```bash
node scripts/setup-telegram-webhook.js menu https://твой-домен.vercel.app/telegram-mini
```

Теперь в боте появится кнопка "🚀 Открыть VibeStudy" которая откроет приложение прямо в Telegram!

---

## 🐛 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
1. Проверь webhook: `node scripts/setup-telegram-webhook.js check`
2. Проверь логи в Vercel Dashboard
3. Проверь переменные окружения
4. Переустанови webhook: `node scripts/setup-telegram-webhook.js auto`

### Проблема: "Webhook verification failed"

**Решение:**
- Проверь что `TELEGRAM_WEBHOOK_SECRET` одинаковый в Vercel и в webhook
- Убедись что секрет установлен при настройке webhook

### Проблема: "Rate limit exceeded"

**Решение:**
- Это нормально для бесплатного плана Vercel
- Пользователь получит сообщение "⏳ Слишком много запросов"
- Лимит: 60 запросов в минуту на пользователя

### Проблема: Бот работает, но команды не выполняются

**Решение:**
1. Проверь что Supabase настроен
2. Проверь миграции БД
3. Проверь логи в `/api/telegram/health`

---

## 📊 Мониторинг

### Health Check

Настрой мониторинг в Vercel:
- Integration → Monitoring
- Или используй внешний сервис (UptimeRobot, Pingdom)
- URL для проверки: `https://твой-домен.vercel.app/api/telegram/health`

### Логирование

Логи доступны в:
- Vercel Dashboard → Deployment → Functions → `/api/telegram/webhook`
- Показывают все ошибки и успешные запросы

### Метрики

Открой в браузере:
```
https://твой-домен.vercel.app/api/telegram/health
```

Получишь:
- Uptime
- Количество обработанных сообщений
- Success rate
- Ошибки по типам
- Популярные команды

---

## 🔐 Безопасность

### ✅ Что уже настроено:
- ✅ Webhook secret verification
- ✅ Rate limiting (60 req/min/user)
- ✅ Input sanitization (XSS protection)
- ✅ RLS в Supabase

### ⚠️ Рекомендации:
- 🔒 Никогда не коммить `.env.local` в git
- 🔄 Меняй токен бота раз в 6 месяцев
- 📝 Используй разные токены для dev/prod
- 🔑 Используй strong webhook secret (32+ символа)

---

## 💰 Стоимость

**Vercel Free Plan:**
- ✅ 100 GB bandwidth/месяц
- ✅ Serverless Functions (100,000 req/день)
- ✅ Достаточно для ~1000 активных пользователей бота

**Если превысил лимиты:**
- Upgrade на Pro план ($20/месяц)
- Или оптимизируй запросы (кеширование, rate limiting)

---

## 📚 Полезные команды

```bash
# Проверить статус webhook
node scripts/setup-telegram-webhook.js check

# Информация о боте
node scripts/setup-telegram-webhook.js info

# Удалить webhook (для локальной разработки)
node scripts/setup-telegram-webhook.js delete

# Настроить menu button
node scripts/setup-telegram-webhook.js menu https://домен.app/telegram-mini

# Проверить menu button
node scripts/setup-telegram-webhook.js menu-check
```

---

## 🎉 Готово!

Теперь твой бот работает **автоматически 24/7** вместе с сайтом на Vercel!

### Что дальше?

1. **Протестируй все команды** бота
2. **Настрой напоминания** в `/remind`
3. **Проверь мониторинг** в `/api/telegram/health`
4. **Поделись ботом** с пользователями!

### Нужна помощь?

- 📖 Документация: `TELEGRAM_BOT_IMPROVEMENTS.md`
- 🔐 Безопасность: `SECURITY_NOTICE.md`
- 💬 GitHub Issues

---

**Успешного запуска! 🚀**
